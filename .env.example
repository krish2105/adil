# Where the shared data store lives. It sits outside every repository in this
# portfolio and is never committed, so each clone points at its own copy.
#
# ADIL expects <DATA_ROOT>/raw/home-credit/ and <DATA_ROOT>/raw/german-credit/.
DATA_ROOT=/path/to/MAIB-Term4/data
